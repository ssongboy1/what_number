# 프로젝트 안내 (AI 에이전트용)

이 문서는 이 저장소에서 작업을 이어받는 사람이나 AI 를 위한 것입니다.

## 무엇을 만드는 중인가

음식점에서 **음식이 나왔는데 몇 번 테이블 것인지 모를 때**, 포스기에서 테이블을
하나씩 눌러보지 않고 최근 주문 목록을 바로 확인하려는 도구입니다.

포스 PC 가 주방 프린터로 보내는 ESC/POS 인쇄 데이터를 **같은 PC 에서 엿듣고**,
전표를 해석해 웹 화면으로 보여줍니다.

사용자는 음식점 사장님이며 개발자가 아닙니다. 설명은 한국어로, 전문용어 없이,
"무엇을 클릭하면 되는지" 수준으로 해야 합니다.

## 왜 이 방식인가 (이미 검토하고 탈락시킨 대안들)

| 방식 | 결론 |
|---|---|
| 포스 벤더 API / 로컬 DB 조회 | **가장 좋은 방법.** 아직 확인 안 됨. 가능하면 이쪽이 정답 |
| 포스 PC 에서 패킷 엿듣기 | **채택.** 추가 장비 0원, 배선·포스 설정 변경 없음 |
| 라즈베리파이 + 미러링 스위치 | 탈락. 사용자가 추가 장비 구매를 원하지 않음 |
| TCP 프록시(포스의 프린터 IP 를 바꿔치기) | 탈락. 프로그램이 죽으면 인쇄가 멈춤 |
| Npcap / 와이어샤크 설치 | 탈락. 사용자가 설치 없는 exe 하나를 원함 |

**지켜야 할 제약** (사용자가 명시적으로 요구한 것):
1. exe 파일 하나로 끝날 것. 별도 설치 프로그램이 없을 것
2. 외부 파이썬 패키지를 쓰지 말 것 (표준 라이브러리만) — 현재 지켜지고 있음
3. 배선·포스 설정을 건드리지 말 것
4. 프로그램이 죽어도 주문서 인쇄에는 영향이 없을 것 (엿듣기만 하므로 충족)
5. 이미지(래스터) 주문서 처리는 **범위 밖**. 단, 글자를 못 읽은 경우
   "이미지로 전송됨" 표시만 남긴다

## 구조

```
launcher.py              exe 진입점 (절대 임포트여야 함 — 아래 함정 참고)
src/what_number/
  sniffer.py     윈도우 raw socket(SIO_RCVALL)으로 패킷 읽기. 관리자 권한 필요
  reassembly.py  TCP 조각 → 전표 한 장. 연결 유지형 포스는 유휴시간으로 구분
  escpos.py      ESC/POS 제어 명령을 길이만큼 건너뛰고 글자만 추출
  orders.py      전표 글자 → 테이블 번호·주문번호·항목
  store.py       SQLite. 여러 프린터로 나간 같은 전표는 한 건으로 묶음
  web.py         주문 화면 + 조회 API (http.server 사용)
  app.py         위를 엮어 실행
  demo.py        포스 없이 화면을 확인하기 위한 가짜 전표 생성
  diagnose.py    COM 포트·포스 폴더·DB 위치 조사 (읽기 전용)
  scan.py        모든 포트를 훑어 주문서가 어디로 나가는지 찾기
  receiver.py    가상 프린터. 포스가 보내는 주문서를 프린터인 척 받는다
```

## 밟았던 함정 (다시 밟지 말 것)

1. **PyInstaller 진입점은 상대 임포트를 쓸 수 없다.**
   `src/what_number/__main__.py` 를 진입점으로 빌드하면 exe 가 실행 즉시
   `ImportError: attempted relative import with no known parent package` 로 죽는다.
   반드시 절대 임포트를 쓰는 `launcher.py` 로 빌드할 것.

2. **한글 출력만으로 프로그램이 죽을 수 있다.**
   콘솔 코드 페이지가 한글을 담지 못하면 `print` 한 번에 `UnicodeEncodeError` 로
   종료된다. `app.setup_console()` 이 이를 막고 있으니 제거하지 말 것.

3. **전표에 로고가 이미지로 들어가는 것은 정상이다.**
   래스터 명령이 있다는 이유만으로 "읽을 수 없음" 표시를 하면 거의 모든 전표에
   잘못된 경고가 뜬다. 글자를 **아예** 못 읽었을 때만 표시한다.

4. **이미지 전표는 글자가 없어 중복 제거 지문이 서로 같아진다.**
   `app._on_job` 에서 데이터 크기를 지문에 섞어 서로 다른 주문으로 남긴다.

5. **CP949 로 출력할 수 없는 글자를 쓰면 안 된다.**
   em dash(—) 같은 글자를 화면에 찍는 순간 한글 윈도우에서 죽는다.
   `tests/test_end_to_end.py` 의 `KoreanEncodingTest` 가 이를 자동으로 막는다.

6. **`input()` 을 그냥 부르면 안 된다.**
   콘솔이 아닌 환경에서 EOFError 가 난다. `app.pause()` 를 쓸 것.

7. **`python` 이 윈도우 스토어 껍데기일 수 있다.**
   사용자 PC 가 실제로 그랬다. 배치 파일은 `py -3` 을 먼저 시도한다.

8. **윈도우에서 `SO_REUSEADDR` 를 쓰면 안 된다.**
   리눅스와 달리 이미 쓰이는 포트에도 그냥 붙어버린다. 프로그램을 두 번 켰을 때
   주문이 두 곳으로 갈라져 조용히 누락된다. 윈도우에서는 `SO_EXCLUSIVEADDRUSE` 로
   열어 충돌이 드러나게 한다.

## 개발

```bash
PYTHONPATH=src python -m unittest discover -s tests   # 테스트 (현재 90건)
PYTHONPATH=src python -m what_number --demo           # 포스 없이 화면 확인
PYTHONPATH=src python -m what_number --replay 파일.bin  # 인쇄 원본으로 인식 시험
PYTHONPATH=src python -m what_number --diagnose       # 이 PC 의 프린터 연결 방식 조사
```

윈도우에서 `python` 이 스토어 껍데기일 수 있으니 `py` 를 쓸 것.

exe 는 `.github/workflows/build-exe.yml` 이 윈도우에서 자동으로 만든다.
**빌드 후 exe 를 실제로 실행해 전표 인식까지 확인하는 단계가 들어 있다.**
이 단계가 위 함정 1, 2 를 잡았으므로 절대 제거하지 말 것.

호환성을 위해 Python 3.8 로 빌드한다(구형 포스 PC). 테스트도 3.8 에서 돌린다.

## ★★ 확정된 사실: 프린터는 물리 시리얼(COM) 연결이다

사용자가 보내온 OKPOS 프린터 설정 화면에서 다음이 확인됐다.

- 포스 제조사는 **OKPOS(오케이포스)**, 프린터종류는 전부 `OKPOS계열`
- `프린터포트(TCP/IP)` 열의 값이 IP 주소가 아니라 `2` `3` `4` `5` 같은 **한 자리 숫자**
- `프린터속도(TCP/PORT)` 열의 값이 **9600** (시리얼 통신 속도로 흔한 값)
- `상시오픈` 이라는 항목이 있음 (시리얼 포트를 계속 열어둘지의 개념)
- 포스 2대(POS번호 01, 02)에 프린터 6대: 파스타·피자브런치·커피·필라프·가니쉬·그릴

**2026-08-28, 포스 PC 의 장치 관리자로 확정됐다.**

```
포트(COM & LPT)
  USB Serial Port(COM5)      <- USB-시리얼 변환기
  통신 포트(COM1) (COM2) (COM3) (COM4) (COM6)
```

"통신 포트(COMx)" 는 윈도우가 물리 RS-232 포트에 붙이는 이름이다.
NPort / Virtual Serial / Serial-over-LAN 은 하나도 없다. 프린터 설정의
프린터포트 값 2,3,4,5 가 COM2·COM3·COM4·COM5 와 정확히 일치한다.

사용자가 "벽에서 나온 랜케이블이 프린터에 연결된다"고 한 것은, 랜케이블을
배선재로만 쓰고 실제로는 RS-232 신호를 흘리는 국내 포스 설치 관행 때문이다.

**따라서 패킷 감시(sniffer.py)는 이 매장에서 아무것도 잡지 못한다.**
sniffer.py / scan.py 는 지우지 말 것 — 네트워크 프린터를 쓰는 환경에서는
그대로 유효하다. 다만 이 사용자에게는 쓸 수 없다.

전표 해석(escpos.py)·주문 추출(orders.py)·저장(store.py)·화면(web.py) 은
그대로 재사용한다. 수집 계층만 교체하면 된다.

### 처음에 무엇을 잘못했나

사용자의 "랜케이블로 연결" 이라는 말을 네트워크 프린터로 단정하고 곧장
구현에 들어갔다. 전제를 먼저 확인했어야 했다. 다음에도 수집 방식을 바꾸기
전에 반드시 실물로 확인할 것.

### 참고: 확인에 쓴 방법

`what_number.exe --scan` 이 가장 확실하다. 포트를 추측하지 않고 모든 TCP 통신을
지켜보면서 ESC/POS 처럼 생긴 데이터가 어느 IP·포트로 가는지 집계해 알려준다.
아무것도 안 잡히면 시리얼 연결이라는 뜻이다.

`what_number.exe --diagnose` (또는 `--진단`) 를 **포스 PC** 에서 실행하면
COM 포트 목록·이름, 프린터로 나가는 TCP 연결 유무, 포스 폴더와 DB 파일 위치가
`data\진단결과_*.txt` 로 저장된다. 그 파일을 받아서 판단할 것.

- COM 포트 이름이 `통신 포트(COM2)` 나 USB 변환기 → **물리 시리얼**. 패킷 감시 불가
- `NPort` / `Virtual Serial` / `Serial-over-LAN` → **네트워크 방식**. 포트 번호만 바꾸면 됨

### 이제 남은 선택지 (우선순위)

-1. **가상 프린터 추가 (--receive)** — 사용자가 제안했고, 현재 가장 유력하다.
   포스 프린터 설정에서 [신규생성] 으로 프린터를 한 대 더 만들어 종류를 TCP/IP,
   주소를 127.0.0.1, 포트를 9100 으로 두면 포스가 주문서를 한 장 더 보내준다.
   `--receive` 가 그것을 받는다. 비용 0원, 관리자 권한 불필요, 기존 프린터 6대는
   손대지 않고 추가만 하므로 그 줄만 지우면 원상복구된다.

   **확인해야 할 것**: 프린터종류 드롭다운에 TCP/IP(네트워크) 항목이 실제로 있는지,
   그리고 **가상 프린터가 응답하지 않을 때 포스가 어떻게 되는지**(조용히 넘어가는지,
   오류창을 띄우는지, 주문이 지연되는지). 이것이 이 방법의 유일한 실제 위험이므로
   반드시 영업 종료 후에 시험할 것.

   상태 조회(DLE EOT / GS r)에 "이상 없음"으로 답하도록 구현해 두었다. 응답이 없으면
   포스가 프린터 고장으로 판단할 수 있기 때문이다. 이 응답을 없애지 말 것.

0. **OKPOS 에 이미 그 기능이 있는지 확인** — 비용 0. 대부분의 포스에는 주문내역·
   영수증 재발행 화면이 있다. 사용자가 원하는 것이 이미 있을 수 있다. 가장 먼저 볼 것
1. **OKPOS 로컬 데이터베이스 직접 읽기** — 최선. 추가 장비가 없고, 프린터 방식의
   한계였던 주문 취소·수정까지 정확히 반영된다. `--diagnose` 결과의
   "주문 데이터가 들어 있을 만한 파일" 로 위치를 찾는다
2. **하드웨어 시리얼 탭** — TX 선을 갈라 USB-시리얼 변환기로 받는다. 완전 수동이라
   인쇄를 방해할 수 없다. 다만 프린터 회선마다 변환기가 필요해 6대면 비용이 든다
3. 드라이버 설치형 시리얼 가로채기 — **권하지 말 것.** 필터 드라이버 설치가 필요하고
   (제약 위반), 가상 포트 방식은 인쇄 경로에 끼어들어 프로그램이 죽으면 인쇄가 멈춘다

**사용자와 범위를 다시 합의한 뒤 진행할 것.** 마음대로 하드웨어를 사라고 하거나
드라이버를 설치하라고 하면 안 된다.

## 지금 상태와 다음 할 일

프로그램과 exe 는 완성되어 있다. 남은 것은 **실제 포스 PC 에서의 첫 실행**이며,
그 결과에 따라 갈린다.

- **주문이 잘 뜬다** → 완료
- **"번호 미확인"** → 사용자에게 `data\dumps` 폴더의 .bin 파일을 받아
  `--replay` 로 확인하고 `config.json` 의 `table_patterns` 에 규칙 추가
- **"이미지로 전송되어 글자를 읽을 수 없습니다"** → 사용자와 범위를 다시 합의한 뒤
  래스터 복원 + OCR, 또는 주문서 이미지를 그대로 표시하는 방식 검토
- **아무것도 안 뜬다** → 프린터가 USB 연결인지, 포트가 9100 이 아닌지,
  포스 PC 가 아닌 곳에서 실행했는지 확인

## 알려진 한계 (사용자에게 이미 고지함)

- 주문 취소·수정은 반영되지 않는다. 프린터로 나간 것만 보이므로 정산용이 아니다
- 배달 전표에는 전화번호·주소가 있다. 그래서 기본 48시간 후 자동 삭제한다
- 안드로이드 태블릿 포스에서는 이 방식이 동작하지 않는다 (윈도우 PC 포스 전용)
